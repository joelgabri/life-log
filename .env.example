POSTGRES_USER=lifelog
POSTGRES_PASSWORD=lifelog
POSTGRES_DB=lifelog

# DATABASE_URL is constructed from the above by default.
# Override here if connecting to an external database.
DATABASE_URL=postgresql://lifelog:lifelog@db:5432/lifelog

# Host port the API is exposed on (default: 8000)
# API_PORT=8000
